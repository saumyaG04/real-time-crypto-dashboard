# Libraries required for the project
dash==2.10.0
requests==2.28.1
pandas==1.5.3

# Install the libraries using:
# pip install -r requirements.txt

# The dashboard can be accessed locally at:
# http://127.0.0.1:8050/ once the application is running on your machine.